<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.emiary.dao.UserDAO">

<!-- 회원정보 저장 -->
<insert id="insert" parameterType="User">
    insert into  users (
		email
		, password
		, nickname
		, birthdate
		, phone
	) 
    values (
		#{email}
		, #{password}
		, #{nickname}
		, #{birthdate}
		, #{phone}
	)
</insert>

<select id="select" parameterType="string" resultType="User">
	select * from users
	where email = #{email}
</select>


</mapper>

